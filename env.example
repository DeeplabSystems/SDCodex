# SDCodex Environment Configuration

# Core settings
SDCODEX_PORT=5001
SECRET_KEY=
MODELS=./models
# User / Group ID (maps container process & created files to host user UID/GID)
PUID=1000
PGID=1000
UMASK=002


# GitHub Personal Access Token (for installing private plugin repositories)
GITHUB_TOKEN=

# Plugin Volumes (automatically managed when plugins are installed via Settings -> Plugins)

